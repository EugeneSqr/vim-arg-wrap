# pylint:disable-all
# comment line goes here
def method_invocation(first, second, third):
    result = first + second + third
    return result

def some_other_func():
    # nowrap
    nowrap = method_invocation(method(a, b, c), [a, b, c], {'a': a, 'b': b, 'c': [a, b, c]})
    # nowrap end
    # a
    a = method_invocation(
        method(a, b, c), [a, b, c], {'a': a, 'b': b, 'c': [a, b, c]})
    # a end
    # b
    b = method_invocation(
        method(a, b, c),
        [a, b, c],
        {'a': a, 'b': b, 'c': [a, b, c]})
    # b end
    # c
    c = method_invocation(method(a, b, c),
                          [a, b, c],
                          {'a': a, 'b': b, 'c': [a, b, c]})
    # c end
